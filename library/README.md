# Library Plugin

Workspace asset library plugin for Moon.

The plugin will contain its UI, manifest, and integration contract in this folder. Moon-specific host adapters and Convex deployment code should remain separate from the plugin package.
